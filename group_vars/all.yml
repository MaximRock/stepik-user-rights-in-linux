---

update_package_cache: true

state:
  present: present
  absent: absent
  directory: directory
  file: file
  touch: touch
  link: link
  restarted: restarted

users:
  - username: alex
    group: devteam
    shell: /bin/bash
    create_home: true
    first_login_change: true
  - username: alice
    group: devteam
    shell: /bin/bash
    create_home: true
    first_login_change: true
  - username: bob
    group: qateam
    shell: /bin/bash
    create_home: true
    first_login_change: true
  - username: charlie
    group: ops
    shell: /bin/bash
    create_home: true
    first_login_change: true
