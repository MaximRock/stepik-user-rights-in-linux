- name: Users Linux
  hosts: all
  become: true
  tags:
    - all

  roles:
    - role: prepare-server
      tags: prepare

    - role: users-and-groups
      tags: users

    - role: access-control
      tags: access

    - role: config-nginx
      tags: nginx

    - role: user-roles-config
      tags: sudoersd

    - role: umask
      tags: umask

    - role: audit
      tags: audit

    - role: ssh
      tags: ssh
